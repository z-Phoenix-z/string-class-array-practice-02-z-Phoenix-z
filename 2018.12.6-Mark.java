a) public void replaceNthOccurrence(String str, int n, String repl){
    int a = findNthOccurence(str,n);
    return currentPhrase.substring(0,a)+repl+currentPhrase.substring(a+a.length());
    }
b) public int findLastOccurrence(String str) 
    
    while (currentPhrase.indexOf()){
    
    }

c) public int findNthOccurrence(String str, int n)
   if (currentPhrase.indexOf(str) = -1){
   return "-1";
   }
   for (int i = 0 ; i < n ; i++){
   
   
